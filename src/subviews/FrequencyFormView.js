// @flow
import React, { Component } from "react";
import { Text, View } from "react-native";

type Props = {};
export default class FrequencyFormView extends Component<Props> {
  render() {
    return (
      <View>
        <Text> FREQUENCY FORM </Text>
      </View>
    );
  }
}
